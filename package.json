{
  "name": "maatram-svms",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:server": "npm run build --workspace server",
    "start:server": "npm run start --workspace server"
  }
}